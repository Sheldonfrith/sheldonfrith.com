import React, {useState, useEffect, useContext, useCallback, useRef} from 'react';
import styles from '../../styles/FancyAnimation.module.css';

interface FancyAnimationProps{

}
const FancyAnimation: React.FunctionComponent<FancyAnimationProps> =({})=> {

return (
<div className={styles.Container}>

</div>
);
}
export default FancyAnimation;
